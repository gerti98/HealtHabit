    <IonItemSliding >
            <IonItem className="IonPadding-0 BgFoto image" style={BgImage} onClick={show}>
                <IonGrid style={{height:"150px"}}>
                    <IonRow className="ion-align-items-center" style={{height:"150px"}}> 
                        <IonCol size="12">
                            <h6 className="IonMarginTop-0" style={{fontWeight:'bold', textAlign:"center", fontSize:"1.5em"}}>Bando {squadra}</h6>
                            {/* <p className="IonMarginBottom-0 IonMarginTop-0">{fonte}</p> */}
                        </IonCol>

                    </IonRow>
                </IonGrid>
            </IonItem>
            {   
                (showElement ? <IonItem className="BgBlack IonPadding-0 descrizioneBando" >
                <IonGrid>
                    <IonRow className="ion-align-items-end"> 
                        <IonCol size="12">
                            <h6 className="IonMarginTop-0" style={{fontWeight:'bold', color: "white"}}>Bando {squadra}</h6>
                            <p className="IonMarginTop-1" style={{color: "white"}}> {descrizione}</p>
                            {(passisti1 != 0) ? <p className="IonMarginTop-1" style={{color: "white"}}>Passisti: {passisti}</p> : <p hidden></p>}
                            {(completi1 != 0) ? <p className="IonMarginTop-1" style={{color: "white"}}>Completi: {completi}</p> : <p hidden></p>}
                            {(scalatori1 != 0) ? <p className="IonMarginTop-1" style={{color: "white"}}>Scalatori: {scalatori}</p> : <p hidden></p>}
                            {(velocisti1 != 0)? <p className="IonMarginTop-1" style={{color: "white"}}>Velocisti: {velocista}</p> : <p hidden></p>}
                            {/* <p className="IonMarginBottom-0 IonMarginTop-0">{fonte}</p> */}
                        </IonCol>

                    </IonRow>
                </IonGrid>
            </IonItem> : <p hidden></p> )
            }
            
        <IonItemOptions side="end">
          <IonItemOption color="danger" className="IonItemsPrincipalOptionFirst" onClick = { () => alert('pressed non mi piace')}>Segnala</IonItemOption>
        </IonItemOptions>
    </IonItemSliding>